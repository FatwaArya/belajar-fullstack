const path = require("path");

module.exports = {
  // config path on src/server
  config: path.resolve("src/server", "config", "database.json"),
  // models path on src/server
  "models-path": path.resolve("src/server", "models"),
  // migrations path on src/server
  "migrations-path": path.resolve("src/server", "migrations"),
  // seeders path on src/server
  "seeders-path": path.resolve("src/server", "seeders"),
};
